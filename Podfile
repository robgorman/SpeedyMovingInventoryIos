# Uncomment this line to define a global platform for your project
platform :ios, '10.0'

target 'Speedy Moving Inventory' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Speedy Moving Inventory
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Crash'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'Alamofire', '~> 4.0'

  
  target 'Speedy Moving InventoryTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Speedy Moving InventoryUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
